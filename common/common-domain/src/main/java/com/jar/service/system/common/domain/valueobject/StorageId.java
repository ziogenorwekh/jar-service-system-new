package com.jar.service.system.common.domain.valueobject;

import java.util.UUID;

public class StorageId extends BaseId<UUID> {
    public StorageId(UUID storageId) {
        super(storageId);
    }
}

package com.jar.service.system.common.domain.valueobject;

import java.util.UUID;

public class AppAnalyzeId extends BaseId<UUID> {
    public AppAnalyzeId(UUID appAnalyzeId) {
        super(appAnalyzeId);
    }
}
